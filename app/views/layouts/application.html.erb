<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1><%= yield(:title) %></h1>
    <p>Emoji emoticons listed on this page are supported on <strong>GitHub, GitLab, Bitbucket</strong></p>
    <%= yield %>
    <div>
      <p>Navigation:</p>
      <p><%= link_to 'Random', root_path %></p>
      <p><%= link_to 'Full list', full_list_path %></p>
    </div>
  </body>
</html>
